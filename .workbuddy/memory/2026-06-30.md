# 2026-06-30

## Phase E: 2025-07 YouTube Backfill (continued)

**Status update**: 16/128 items completed, validation passing for items 1-16.

### Progress
- Items 1-12 (pre-existing): 9 valid + 3 newly written (8u3, 9fxg, 9reE, 9XM8, aGbB) = 12 total valid before resuming
- Items 12-16 written today: 8u3JHl1NV1g (Trump tariffs/Big Beautiful Bill), 9fxg05UfQkI (Parker Conrad/Rippling), 9reEXWDXk-4 (Carlyle CEO), 9XM8yDN-AEA (Hugging Face), aGbBiWcAMzw (Goolsbee)
- All passed validation after fixing number-mismatch issues
- **Current count**: 28 valid / 13 invalid / 114 missing (out of 155)

### Critical learnings on validation (DURING SESSION)
- The number regex `\$?\d[\d,]*(?:\.\d+)?%?` matches digits in BOTH value and context
- **Numbers in value+context must all appear in transcript exactly** (or as substring, e.g. "517" not "5170")
- Common gotchas to avoid:
  - "1.5万" in Chinese context gets parsed as "1.5" — must use "$15" form when transcript says "15 20k"
  - "5170亿美元" extracted as "5170" — but transcript only has "517" — match exact digit sequence
  - "8万美元" extracted as "8" — transcript has "$80,000" but NOT "8" alone
  - "5个" in Chinese context gets parsed as "5" — use words like "数个" or check transcript
  - "1.8亿" extracted as "1.8" — use "$180 million" form to match transcript
- **The two L's: `lstrip("$")` + `replace(",", "")`** in `numbers()` function
- Pattern that works: write `value` field using English form (e.g. "$180 million", "15 20k", "517 billion") that matches transcript verbatim, and put Chinese interpretation in `context` carefully avoiding stray digits

### Script files used
- `_read_next2.py 12` — read next pending item
- `_check_status2.py` — initial status check
- Validation: `python scripts/backfill/validate_summary_outputs.py --platform youtube --item-id youtube:VIDEO_ID`
- Then: `python scripts/backfill/validate_summary_outputs.py --platform youtube --month 2025-07` for monthly status

### Remaining work
- 114 items still need summaries (items 17-128)
- 13 invalid items need fixes (8m9N, 8Pg0 with sha256_mismatch; various key_facts issues)
- Need faster workflow to get through the backlog

## May 2026 YouTube Batch 2 Processing

**Batch 2 completed** (174 total, 15 done, 159 remaining):
- 5_6gNNKkkAk: AI and Autonomous Weapons (Interesting Times, Anduril) — score 5
- 619uvwZH17E: Bloomberg Surveillance 5/8/2026 (jobs/Iran/Fed) — score 3
- 673Low-Hj5E: Can AI Make People Feel Less Lonely? (The Daily, ELI Q robot) — score 4
- Next batch: 6LkMI6uvqZY, 7-HmjiXKlvg, 74Z22u-zc1Q (→ batch 3 below)

Direct JSON writing via Write tool avoids Windows GBK encoding corruption in Python scripts.

## May 2026 YouTube Batch 3 Processing

**Batch 3 completed** (174 total, 18 done, 156 remaining, 10.3%):
- 6LkMI6uvqZY: Jane Street揭秘 (Bloomberg Originals) — score 5
- 7-HmjiXKlvg: 伊朗中国AI战争 (Invest Like the Best, DoD advisor Darren) — score 7
- 74Z22u-zc1Q: 特朗普的党内异见者 (The Daily, Massie vs Gallrein primary) — score 5

### Validation format lessons (critical for future batches):
- `platform`, `platform_id`, `transcript_sha256` must be at JSON TOP level (not just inside metadata)
- `summary`, `core_points`, `takeaways`, `guests`, `topics`, `tensions` → arrays of strings
- `key_facts` → array of {label, value, context} objects
- `quote` → {text, speaker, kind} object
- Numbers in key_facts value/context must match transcript numbers exactly (use "39 billion" not "390亿美元")
- Use DB sha256 values (differ from locally computed)
- takeaways: exactly 1-2 items; longer videos (>=1800s) need 3+ summary paragraphs and 4+ core_points
- Next batch: 7bWqp3oZyXg, 7PMbCNH4vOo, 8IKy6o8NsC4
